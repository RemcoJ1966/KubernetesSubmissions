apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: project
  name: todoapp-claim
spec:
  storageClassName: standard-rwo
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
